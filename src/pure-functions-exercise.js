// () => console.log('hey friend!');

// () => 1 + 2;

// () => Math.random();

// (val) => count + val;

// (amount, discount) =>
//    amount * (1 - discount);

// (array) => array.sort();

// (array) => array = [1,2,3];

// () => Date.now();
